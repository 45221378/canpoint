var bar = function (a,b) {
  if(a.indexOf(b)>=0){
    return true
  }else{
    return false
  }
}
  

var change = function (a) {
  // var newStr = a.replace("img", "image");
  // console.log(newStr)
  // return newStr;
}

module.exports = { bar: bar,change:change};