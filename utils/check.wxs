var bar = function (a,b) {
  if(a&&b){
    // console.log(a);
    // console.log(b);
    if(a.indexOf(b)>=0){
      return true
    }else{
      return false
    }
  }
}
  
module.exports = { bar: bar};